# Copy to .deploy.env in the repo root (gitignored) and fill in your cPanel SSH values.
# Then run: powershell -ExecutionPolicy Bypass -File scripts/deploy-ssh.ps1

EASYINTERN_SSH_TARGET=user@your-server.hostname
EASYINTERN_REMOTE_WEB_ROOT=/home/username/public_html
